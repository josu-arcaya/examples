[project]
name = "airflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "apache-airflow-providers-postgres>=6.2.0",
    "apache-airflow[postgres,postgresql]>=3.0.2",
    "psycopg2-binary>=2.9.10",
]
